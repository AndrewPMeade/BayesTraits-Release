#if !defined (TREENODESHEADDER)
#define TREENODESHEADDER

#include "typedef.h"

void	SetPartitions(TREES *Trees);
void	FreePartitions(TREES *Trees);
void	SetRecNodes(RECNODE RNode, OPTIONS *Opt);

#endif

